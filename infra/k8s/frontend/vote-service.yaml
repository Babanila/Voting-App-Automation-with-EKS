apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: babs-app
  labels:
    app: vote
spec:
  type: ClusterIP
  selector:
    app: vote
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
