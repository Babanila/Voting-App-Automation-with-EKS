apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: babs-app
  labels:
    app: vote
spec:
  type: NodePort
  selector:
    app: vote
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080  # Vote on port 8080 -> NodePort 30080
      protocol: TCP
