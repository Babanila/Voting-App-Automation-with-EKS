apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: ${NAMESPACE}
  labels:
    app: result
spec:
  type: ClusterIP
  selector:
    app: result
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
