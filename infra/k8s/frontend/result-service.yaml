apiVersion: v1
kind: Service
metadata:
  name: result-service
  namespace: babs-app
  labels:
    app: result-service
spec:
  type: ClusterIP
  selector:
    app: result
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
