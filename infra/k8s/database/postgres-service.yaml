apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: babs-app
  labels:
    app: postgres-service
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
